RewriteEngine On

# Allow normal access to files/folders
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Redirect all requests to ecommerce/index.php
RewriteRule ^ index.php [QSA,L]